# เอกสาร Test Case
**โปรเจค:** Buggy Todo App

## 📋 Test Cases (กรณีทดสอบ)

| รหัส (TC ID) | ฟีเจอร์ (Feature) | คำอธิบาย (Description) | เงื่อนไขเริ่มต้น (Preconditions) | ขั้นตอนการทดสอบ (Steps) | ผลลัพธ์ที่คาดหวัง (Expected Result) |
|---|---|---|---|---|---|
| **TC-01** | เพิ่ม Todo | ตรวจสอบการเพิ่มรายการ Todo ใหม่ผ่านหน้าเว็บ | รันเซิร์ฟเวอร์เรียบร้อย | 1. พิมพ์ข้อความลงในช่อง Input<br>2. กดปุ่ม Add หรือกด Enter | รายการใหม่แสดงบนหน้าจอและถูกบันทึกลงฐานข้อมูล |
| **TC-02** | ลบ Todo | ตรวจสอบการลบรายการ Todo ที่มีอยู่ | มีรายการ Todo อย่างน้อย 1 รายการ | 1. กดปุ่มลบ (Delete) ที่รายการ Todo | รายการถูกลบออกจากหน้าจอและฐานข้อมูล |
| **TC-03** | อัปเดตสถานะ | ตรวจสอบการติ๊กถูกเพื่อเปลี่ยนสถานะ (เสร็จ/ยังไม่เสร็จ) | มีรายการ Todo อย่างน้อย 1 รายการ | 1. กด Checkbox ที่รายการ Todo | สถานะเปลี่ยนบนหน้าจอและถูกอัปเดตในฐานข้อมูล |
| **TC-04** | API: สร้าง Todo | ตรวจสอบ POST `/api/todos` ทำงานถูกต้อง | เซิร์ฟเวอร์ Backend รันอยู่ | 1. ยิง POST Request พร้อมแนบ body | เซิร์ฟเวอร์ตอบกลับ 201 และเซฟข้อมูลสำเร็จ (ไม่มี Error) |
| **TC-05** | API: ลบ Todo | ตรวจสอบ DELETE `/api/todos/:id` ทำงานถูกต้อง | เซิร์ฟเวอร์ Backend รันอยู่ | 1. ยิง DELETE Request พร้อมส่ง id | เซิร์ฟเวอร์ตอบกลับ 200 และลบข้อมูลสำเร็จ (ไม่มี Error) |
