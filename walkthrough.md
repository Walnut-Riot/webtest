# Buggy Todo App (AI Debugging Testbed)

ผมได้สร้างโปรเจคจำลองระบบ Todo List ที่มีหน้าตาสวยงาม แต่แฝงไปด้วย **บั๊ก (Bugs) ระดับเบสิกไปจนถึงระดับที่ต้องใช้ความเข้าใจลอจิก** ถึง 8 จุด เพื่อให้คุณเอาไว้ใช้ทดสอบความสามารถในการรีวิวโค้ดและแก้บั๊กของ Local AI ของคุณครับ!

## 📁 โฟลเดอร์โปรเจค
โค้ดทั้งหมดอยู่ที่: `C:\laragon\www\Test\buggy-todo-app`
คุณสามารถเปิดโฟลเดอร์นี้ แล้วให้ AI ของคุณทำการแสกนดูโค้ดไฟล์ `server.js` และ `public/app.js` ได้เลยครับ

## ⚙️ วิธีการรันเพื่อดูอาการบั๊ก
1. เปิด Terminal ชี้ไปที่ `C:\laragon\www\Test\buggy-todo-app`
2. พิมพ์คำสั่ง `node server.js`
3. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
4. ลองกดเพิ่มข้อมูล กดลบ และกดติ๊กถูกดู คุณจะพบว่า **แอปทำงานรวนไปหมดเลยครับ!**

---

## 🐞 เฉลยบั๊กทั้งหมดที่มีในโปรเจคนี้ (Solutions)
*(คุณสามารถเก็บหน้านี้ไว้ตรวจคำตอบของ AI ของคุณได้ครับ)*

### ฝั่ง Backend (`server.js`)
- **[BUG 1]** บรรทัด 45: `saveToDatabase(newTodo);` ขาดการใช้คำสั่ง `await` ไป ทำให้ระบบไม่รอเซฟลงฐานข้อมูลจำลอง (Race Condition)
- **[BUG 2]** บรรทัด 48: คืนค่าเป็นตัวแปรที่ไม่ได้ประกาศ `res.json(resultData);` (จะทำให้ Server พังเพราะ ReferenceError)
- **[BUG 3]** บรรทัด 55: ตัวแปร `id` ที่รับมาจากพารามิเตอร์เป็นประเภท String แต่ไปใช้เช็ค `t.id !== id` กับ Number ทำให้กดลบเท่าไหร่ก็ลบไม่ออก!
- **[BUG 4]** บรรทัด 58: ใช้ชื่อตัวแปรผิด `console.log(todoId)` (ต้องเป็น `id`)
- **[BUG 5]** บรรทัด 67: ลอจิกอัปเดตสถานะใช้ `.map()` เฉยๆ โดยไม่เก็บค่าทับตัวแปรเดิม (`todos = todos.map(...)`) ทำให้เวลากดติ๊กถูก สถานะจะไม่ถูกบันทึกจริง

### ฝั่ง Frontend (`public/app.js`)
- **[BUG 6]** บรรทัด 40: ไม่ได้ใส่ `e.preventDefault()` ทำให้เวลากด Enter หรือปุ่ม Add แล้วหน้าเว็บจะรีเฟรชตัวเองทันที
- **[BUG 7]** บรรทัด 47: พิมพ์ URL ผิดเป็น `api/todo` (ที่ถูกคือ `/api/todos`) และ **ไม่ได้ใส่ Headers** ทำให้ Backend อ่านค่า body ไม่ได้ 
- **[BUG 8]** บรรทัด 54: ไปเรียก `loadTodos()` ซ้ำซ้อนทั้งที่หน้าเว็บรีเฟรชตัวเองไปแล้วเพราะบั๊กที่ 6

> [!TIP]
> ลองเอาโฟลเดอร์นี้โยนให้ Local AI ของคุณ แล้วบอกมันว่า *"ช่วยเช็คโปรเจค Node.js Todo App นี้ให้หน่อย มันมีบั๊กทั้งหน้าบ้านและหลังบ้าน ทำให้เพิ่ม ลบ หรือแก้ไขข้อมูลไม่ได้เลย"* แล้วมาดูกันว่า AI ของคุณจะหาเจอครบทั้ง 8 จุดไหม!
